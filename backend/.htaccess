# ── LiteSpeed/Apache CORS Fix for Hostinger Node.js ──────────────────────
#
# KEY: mod_headers MUST come before RewriteEngine for LiteSpeed to apply
# them to internally-generated (RewriteRule R=204) responses.
#

<IfModule mod_headers.c>
    # Applied to ALL responses including the OPTIONS 204 below
    Header always set Access-Control-Allow-Origin "https://aptiogen-56f98.web.app"
    Header always set Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE, PATCH"
    Header always set Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With, Accept, Origin"
    Header always set Access-Control-Allow-Credentials "true"
    Header always set Access-Control-Max-Age "3600"
</IfModule>

RewriteEngine On

# Pass Authorization header through to Node.js (LiteSpeed strips it by default)
RewriteCond %{HTTP:Authorization} ^(.+)$
RewriteRule .* - [E=HTTP_AUTHORIZATION:%1]

# Handle OPTIONS preflight: 204 No Content with CORS headers set above
RewriteCond %{REQUEST_METHOD} ^OPTIONS$
RewriteRule .* - [R=204,L]
